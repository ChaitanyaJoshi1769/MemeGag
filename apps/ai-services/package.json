{
  "name": "@memegag/ai-services",
  "version": "0.1.0",
  "description": "AI and ML services for meme generation and analysis",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "dev": "nest start --watch",
    "build": "nest build",
    "start": "node dist/main.js",
    "docker:build": "docker build -t memegag-ai-services:latest .",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@memegag/config": "*",
    "@memegag/database": "*",
    "@memegag/logger": "*",
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "axios": "^1.6.0",
    "openai": "^4.0.0",
    "reflect-metadata": "^0.1.13",
    "rxjs": "^7.8.0"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.0.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}

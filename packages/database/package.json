{
  "name": "@memegag/database",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:generate": "prisma generate",
    "db:seed": "node --loader tsx prisma/seed.ts",
    "db:studio": "prisma studio",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "prisma": "^5.0.0",
    "typescript": "^5.4.0",
    "tsx": "^4.0.0"
  }
}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: creator-service
  namespace: memegag
  labels:
    app: creator-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: creator-service
  template:
    metadata:
      labels:
        app: creator-service
    spec:
      containers:
      - name: creator-service
        image: ghcr.io/chaitanyajoshi1769/memegag-creator-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3007
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: memegag-config
              key: NODE_ENV
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: memegag-config
              key: LOG_LEVEL
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: memegag-secrets
              key: DATABASE_URL
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3007
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /health
            port: 3007
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: creator-service
  namespace: memegag
spec:
  selector:
    app: creator-service
  ports:
  - protocol: TCP
    port: 3007
    targetPort: 3007
  type: ClusterIP
